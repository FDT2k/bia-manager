

CREATE TABLE IF NOT EXISTS subjects (
	id INTEGER PRIMARY KEY AUTOINCREMENT,
   	lastname TEXT ,
	firstname TEXT,
    birthdate TEXT,
    age INTEGER,
    gender TEXT,
    groups TEXT,
    usual_height FLOAT,
    usual_weight FLOAT,
    uuid TEXT,
    diag TEXT

     
);